spring:
  datasource:
    url: jdbc:mysql://localhost:3306/student_manage?createDatabaseIfNotExist=true
    username: root
    password: 2003
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server :
  port : 8080